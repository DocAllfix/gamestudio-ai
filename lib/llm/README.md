# lib/llm/ — Workstream W2

OpenRouter + Helicone router with retry, cost cap and structured-output Zod.

Owner: **W2** (`ws/w2-tools-llm`).
Branch policy: write only on this workstream's branch.
Read-only on every other branch.

Source contract: `lib/contracts/` (do not modify without a contract
proposal issue, see Supreme Plan §01 Rule 1).

Phases: see `docs/SUPREME_CONCURRENT_EXECUTION_PLAN.md` §04 phase 2.1.

Logging: append `data/standup/W2-YYYY-MM-DD.jsonl`
