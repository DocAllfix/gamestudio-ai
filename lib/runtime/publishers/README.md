# lib/runtime/publishers/ — Workstream W3

itch.io packager via butler CLI (Steam pipeline = Phase 2).

Owner: **W3** (`ws/w3-runtime-engines`).
Branch policy: write only on this workstream's branch.
Read-only on every other branch.

Source contract: `lib/contracts/` (do not modify without a contract
proposal issue, see Supreme Plan §01 Rule 1).

Phases: see `docs/SUPREME_CONCURRENT_EXECUTION_PLAN.md` §05 phase 3.11.

Logging: append `data/standup/W3-YYYY-MM-DD.jsonl`
